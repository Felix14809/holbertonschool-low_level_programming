#include "main.h"
/**
 * mul - function to take 2 inputs and multiply them
 * @a: first integer to be multiplied
 * @b: second integer to be multiplied
 *
 * Return: the sum
 */
int mul(int a, int b)
{
	return (a * b);
}
