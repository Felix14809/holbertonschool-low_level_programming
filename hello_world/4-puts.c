#include <stdio.h>
/**
 * main - a simple print function
 * puts automatically adds a newline at the end
 *
 * Return: sets the value back to 0
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
