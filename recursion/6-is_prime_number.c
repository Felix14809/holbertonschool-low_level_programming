#include "main.h"
/**
 * is_prime_number - calculates if the number is prime
 *@n: number to check
 *Return: 1 if yes otherwise 0
 */
int is_prime_number(int n)
{
	int i = 1;
	float prime
	if ((is_prime_number(n) / i) == (int))
}